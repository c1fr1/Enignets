#version 400 core

layout (location = 0) in vec3 vertices;

out vec4 vecColor;

uniform mat4 matrix;

void main() {
	gl_Position = matrix * vec4(vertices, 1f);
    vecColor = vec4(vertices.y/5, (5-vertices.y)/5, 0, 1);
}